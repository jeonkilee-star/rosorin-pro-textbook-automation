# 교재개발 정책

이 문서는 ROSOrin Pro 교재자동화 파이프라인의 트랙·콘텐츠·배포 정책을 정리한다. 2026-09-09 세션에서 수립.

## 1. 트랙 정의 (대학 / 대학교)

`render.js`의 `--track jeondae|daehak` 두 트랙은 다음 기준으로 구분한다.

| 축 | 대학 (`jeondae`) | 대학교 (`daehak`) |
|---|---|---|
| 교육 목적 | NCS 기반 직무능력 습득 → 취업/자격 | 학문적 이해 + 응용력 → 전공 심화 |
| 선수지식 가정 | 없음/최소 (순차적 절차 학습) | 선형대수·제어이론 기초 있음 가정 |
| "안다"의 기준 | 이 코드가 뭘 하는지, 순서대로 재현 가능 | 왜 이렇게 동작하는지, 파라미터를 바꿔 설계 변형 가능 |
| 평가 방식(가정) | 체크리스트/실기 시연 | 보고서·수식 유도·설계 근거 서술 |

레슨 20개 전부 두 트랙에 동일하게 포함된다. 차이는 **심화 이론 블록(`theoryDeepDive`)의 유무뿐**이다.

## 2. 심화 이론(`theoryDeepDive`) 삽입 기준

다음 중 하나를 충족하면 대학교 트랙 전용 심화 이론 블록을 추가한다:

- **(a) 수식 모델형**: 닫힌 형태의 수식/모델이 존재하고, 그걸 알면 파라미터 튜닝·오류 진단·타 하드웨어 이식이 가능해지는 경우
- **(b) 설계원칙형**: 수식은 없지만, 이해하면 시스템을 진단·수정·이식할 수 있게 되는 설계원칙(예: 보안 취약점 클래스, 프롬프트 엔지니어링 원리)인 경우

2026-09-09 감사 결과 적용 대상(6개):

| 레슨 | 유형 | 내용 |
|---|---|---|
| 03-2 (바퀴) | (a) | 메카넘 휠 역기구학 |
| 08-1 (라이다) | (a) | PID 제어 수식 |
| 14-1 (물체분류) | (a) | 카메라 좌표변환(동차좌표) |
| 12-1 (LLM이동) | (b) | 프롬프트 엔지니어링·eval() 보안 |
| **10-1 (YOLO)** | (a) | 지수이동평균(EMA) — 2026-09-09 신규 |
| **20-1 (사람따라가기)** | (a) | 데드존/히스테리시스 제어 — 2026-09-09 신규 |

07-1·20-1도 PID를 재사용하지만(08-1과 동일 개념), **의도적으로 교차링크를 넣지 않고 각 레슨을 독립적으로 유지**하기로 결정함(2026-09-09).

새 레슨을 추가할 때마다 이 기준으로 재검토한다.

## 3. 선수지식 링크(`prerequisites`) — 트랙 무관, 초점 정확도 필수

- 트랙 게이트 없음 — 대학·대학교 두 트랙 모두에 노출 (실무 트랙 학생도 배경지식이 필요할 수 있음)
- 검증 기준: 무료 접근성 / 출처 신뢰도 / 언어(한국어 우선) / 최신성 / **초점 정확도(최우선)** — 코스 전체/재생목록 착지 금지, 반드시 필요한 개념 지점에 정확히 착지(타임스탬프·앵커·`focus` 텍스트)
- 데이터 모델:
```json
"prerequisites": [
  { "topic": "...", "url": "...", "focus": "...", "source": "...", "verifiedAt": "YYYY-MM-DD" }
]
```
- 상태: **10/20개 레슨 완료** (2026-09-09) — `render.js`에 트랙 무관 렌더링 로직 추가.
  - 파일럿 4개: 03-2(메카넘 역기구학)·08-1(PID)·12-1(eval보안)·14-1(동차좌표)
  - 확장 6개: 02-1(ROS2 노드·토픽)·06-1(LAB 색공간)·10-1(confidence 임계값)·15-1(MediaPipe Hands)·17-1(function calling)·19-1(MediaPipe Pose)
  - 제외(의도적): 07-1·20-1 — 08-1과 PID 개념이 겹쳐 중복 링크 추가하지 않기로 결정(2026-09-09)
  - 나머지 8개(00-2·03-1·09-1·11-1·13-1·16-1·18-1·21-1)는 배경지식 격차가 뚜렷하지 않다고 판단해 보류 — 필요 시 같은 기준(초점 정확도)으로 추가

## 4. 교수용 지도서 원칙

1. **접근 통제가 최우선** — 콘텐츠보다 먼저 결정. 학생판과 물리적으로 다른 배포 채널 필수 (현재 GitHub Pages는 완전 공개라 그대로 쓸 수 없음)
2. **지도서는 학생판 위의 추가 레이어** — 별도 문서로 관리하지 않고 레슨 JSON에 지도서 전용 필드(`answerKey`, `pacingTip`, `commonMistakes`, `rubric`)를 추가해 렌더링 단계에서 분기
3. **정답은 반드시 근거(파일:줄번호)를 단다** — 이 저장소의 기존 관행과 동일
4. **항목 범위**: `checkQuestions` 정답·`recordTable` 예상값은 지도서 전용 신규 작성. `evaluation`/`troubleshooting`은 이미 학생판에 공개돼 있어 지도서 전용이 아님
5. **버전 동기화** — 학생판 갱신 시 지도서도 파이프라인 재실행으로 함께 갱신, 수동 이원화 금지

### 접근 통제 — 검증된 업계 패턴 (2026-09-09 스터디)

미국 교과서 출판사(Pearson 등)·한국 출판사(NEIS 인증)·GitHub Education 모두 공통적으로 **"신청 → 소속 증빙 → (자동/수동) 검증 → 승인 후 접근"** 구조를 쓴다. 셀프 가입 즉시 접근 사례는 없음 — 검증 단계의 마찰이 남용 방지 장치.

### 인증 방식 — 확정 (2026-09-09)

| 항목 | 결정 |
|---|---|
| 구현 위치 | svrobotics-website (Next.js, Vercel 배포) — 별도 인증 서비스 계약 불필요, 기존 인프라 재사용 |
| 신청 경로 | `/instructor/apply` 신청 폼 (이름·소속기관·담당과목·이메일) |
| 승인 방식 | 회사 수동 승인 (셀프 가입 없음 — 2절 사례 스터디의 공통 패턴 그대로 적용) |
| **로그인 방식** | **이메일 매직링크** — 비밀번호 관리 부담 없음, 승인된 이메일로 링크 발송 → 클릭 한 번으로 로그인 |
| **승인 담당자** | **미정 — 지정 필요.** 담당자 확정 전까지는 신청이 들어와도 처리할 사람이 없는 상태이므로, 실제 신청 폼을 오픈하기 전에 반드시 지정할 것 |
| 구현 착수 | **정책만 확정, 실제 코딩은 별도 세션에서 진행** (2026-09-09 세션 범위 아님) |

**다음 세션 착수 시 필요한 것**: 승인 담당자 이메일(환경변수로 관리), 매직링크 발송 이메일 서비스(예: Resend, Vercel과 연동 쉬움) 선정.

### 콘텐츠 저장 위치 — 비공개 저장소 분리 (2026-09-09)

이 저장소(`rosorin-pro-textbook-automation`)는 **완전 공개**라서, 정답이 들어가는 지도서 콘텐츠를 여기(`lessons/*.json`)에 직접 넣으면 웹사이트 인증과 무관하게 저장소 자체에서 그대로 노출된다. 그래서 정답·채점기준 등 지도서 전용 콘텐츠는 **별도 비공개 저장소** `jeonkilee-star/rosorin-pro-textbook-instructor`에 분리 보관한다.

- 비공개 저장소는 이 저장소를 **읽기 전용으로만 참조**(학생판 레슨 JSON) — 이 저장소 쪽에는 지도서 콘텐츠가 전혀 들어오지 않음
- 파일럿 2개 레슨(00-2, 08-1) 완료, 나머지는 필요 시 확장
- 상세 구조·작성 원칙은 비공개 저장소의 README.md 참고 (접근 권한 있는 사람만 열람 가능)

## 5. e북 유통 (검토만, 미결정)

- 인터랙티브 요소(`<input>` 기록표, 플로팅 목차)가 EPUB/Kindle에서 대부분 소실됨
- 지금은 상시 갱신 파이프라인 — e북은 ISBN 붙은 확정판 전제라 릴리즈 스냅샷 체계 필요
- 벤더(Hiwonder) 소스코드 인용이 무료 교육 목적으로는 문제없었지만, 유료 e북이라면 라이선스 재검토 필요
- 목적(무료 확산 vs 유료화)에 따라 우선순위 다름 — **미결정**
